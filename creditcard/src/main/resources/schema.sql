create table credit_card(
     pan varchar(255) PRIMARY KEY NOT NULL,
     first_name varchar(255),
     last_name varchar(255),
     card_number varchar(255),
     expiry_date varchar(255),
     sec_code int
);